name: CI básico

on:
  push:
    branches: [ "main" ]

jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar mensaje
        run: echo "✅ El flujo se ejecutó correctamente en la rama main"
